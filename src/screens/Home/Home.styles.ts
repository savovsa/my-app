import { Styles } from "../../style/style.types";

const styles: Styles = {
  container: theme => ({ padding: theme.spacing.M }),
  heading: theme => ({ color: theme.colors.primary })
};

export default styles;
